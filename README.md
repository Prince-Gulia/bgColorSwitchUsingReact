# 🎨 Background Color Changer (React)

A simple React project that dynamically changes the background color of the page using reusable button components and the `useState` hook.

This project was built to understand:
- React component structure
- State management using `useState`
- Passing props and handling events
- Basic Tailwind CSS styling

---

## 🚀 Features

- Change background color with a single click
- Reusable `Button` component
- Smooth color transition
- Clean and minimal UI
- Built with React + Tailwind CSS

---

## 🧠 Concepts Used

- React `useState` hook
- Props drilling (`setColor` passed from parent to child)
- Event handling with `onClick`
- Inline styles with dynamic values
- Tailwind CSS utility classes

---

## 🛠️ Tech Stack

- React (Vite)
- JavaScript (ES6)
- Tailwind CSS

---

## 📂 Project Structure

